on: push
name: cargo fmt

jobs:
  cargo_fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
            persist-credentials: false

      - name: Run cargo fmt
        run: cargo fmt --all --check
